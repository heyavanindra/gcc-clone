/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['firebasestorage.googleapis.com','sahara-theme.myshopify.com', 'cloud.appwrite.io',"res.cloudinary.com"],
      }
};

export default nextConfig;
